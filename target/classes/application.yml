spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/currencydb
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true

openexchangerates:
  app-id: a6d8f72a652a471ca0a2238d43daa3fd
